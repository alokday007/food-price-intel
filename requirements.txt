# Food Price Intelligence — pinned dependencies (full list per SPEC §14).
# Phase 0 only exercises Django, psycopg[binary], python-dotenv, gunicorn,
# but the whole list is pinned here so deps are fixed once.

# --- Web framework ---
Django==5.1.4
djangorestframework==3.15.2
django-htmx==1.21.0
gunicorn==23.0.0

# --- Database driver (psycopg 3) ---
psycopg[binary]==3.2.3

# --- Data / ETL ---
pandas==2.2.3
numpy==2.1.3
requests==2.32.3
openpyxl==3.1.5

# --- ML ---
statsmodels==0.14.4
lightgbm==4.5.0
scikit-learn==1.5.2
joblib==1.4.2

# --- Async / broker / cache ---
celery==5.4.0
redis==5.2.0

# --- AI layer ---
anthropic==0.39.0

# --- Config ---
python-dotenv==1.0.1

# --- Testing ---
pytest==8.3.4
pytest-django==4.9.0
